{
  "name": "ballpark-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "normalize-plans:dry": "node scripts/normalize-plans.js",
    "normalize-plans:apply": "node scripts/normalize-plans.js --apply"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "expo-server-sdk": "^3.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
